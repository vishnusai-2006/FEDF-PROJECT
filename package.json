{
  "name": "fefd-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm:dev --prefix frontend\" \"npm:start --prefix backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
